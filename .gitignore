# Build output
bin/
obj/
publish/
dist/
dist-server/
# temporary verify/publish/audit output dirs used during local builds
_verifybuild/
_pubbuild/
_pub/
_audit/
_vs/
_vd/
_vr/
_v/

# Local tooling / scratch
temp/

# Runtime data (downloads, extracted files, logs, session, settings)
data/
**/data/

# IDE
.vs/
.idea/
*.user

# OS
Thumbs.db
.DS_Store

# Local dev config (may hold secrets — e.g. the DB password)
**/appsettings.Development.json
**/appsettings.Production.json
**/appsettings.*.Local.json

# Generated deployment secrets (signing private key, HMAC key, TLS pfx + password, filled prod config)
_secrets/
