<UserControl x:Class="DataVortex.App.Views.StatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI"
             TextElement.Foreground="{DynamicResource TextPrimary}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Stats" Style="{StaticResource Title}" DockPanel.Dock="Left"/>
            <Button Content="Refresh" Style="{StaticResource Ghost}" Command="{Binding RefreshCommand}"
                    DockPanel.Dock="Right" HorizontalAlignment="Right"/>
        </DockPanel>

        <!-- Summary cards -->
        <UniformGrid Grid.Row="1" Columns="5" Margin="0,0,0,4">
            <Border Style="{StaticResource Card}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="ARCHIVES" Style="{StaticResource MetricLabel}"/>
                    <TextBlock Text="{Binding TotalRecords}" Style="{StaticResource Metric}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="DATA" Style="{StaticResource MetricLabel}"/>
                    <TextBlock Text="{Binding TotalBytes, Converter={StaticResource Bytes}}" Style="{StaticResource Metric}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="COMPLETED" Style="{StaticResource MetricLabel}"/>
                    <TextBlock Text="{Binding Completed}" Style="{StaticResource Metric}" Margin="0,4,0,0"
                               Foreground="{DynamicResource Success}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="IGNORED" Style="{StaticResource MetricLabel}"/>
                    <TextBlock Text="{Binding Ignored}" Style="{StaticResource Metric}" Margin="0,4,0,0"
                               Foreground="{DynamicResource Muted}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource Card}" Margin="0,0,0,0">
                <StackPanel>
                    <TextBlock Text="FAILED" Style="{StaticResource MetricLabel}"/>
                    <TextBlock Text="{Binding Failed}" Style="{StaticResource Metric}" Margin="0,4,0,0"
                               Foreground="{DynamicResource Error}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Per-channel -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,12,0,0" Padding="12" Height="220">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="PER CHANNEL" Style="{StaticResource MetricLabel}" Margin="0,0,0,8"/>
                <DataGrid ItemsSource="{Binding Channels}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Channel" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Archives" Binding="{Binding Count}" Width="110"/>
                        <DataGridTextColumn Header="Data" Binding="{Binding TotalBytes, Converter={StaticResource Bytes}}" Width="110"/>
                        <DataGridTextColumn Header="Failed" Binding="{Binding Failed}" Width="90"/>
                        <DataGridTextColumn Header="ID" Binding="{Binding ChannelId}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <!-- Search -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,12,0,0" Padding="12">
            <DockPanel>
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10" LastChildFill="False">
                    <TextBlock Text="SEARCH HISTORY" Style="{StaticResource MetricLabel}" DockPanel.Dock="Left"
                               VerticalAlignment="Center"/>
                    <Button Content="›" Style="{StaticResource Ghost}" Command="{Binding NextPageCommand}"
                            IsEnabled="{Binding CanNext}" DockPanel.Dock="Right" Padding="10,3"/>
                    <Button Content="‹" Style="{StaticResource Ghost}" Command="{Binding PrevPageCommand}"
                            IsEnabled="{Binding CanPrev}" DockPanel.Dock="Right" Padding="10,3" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding PageInfo}" Style="{StaticResource Caption}"
                               DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <ComboBox ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding StatusFilter}" Width="130"
                              DockPanel.Dock="Right" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}" Width="320"
                             DockPanel.Dock="Left" Margin="16,0,0,0"/>
                </DockPanel>
                <DataGrid ItemsSource="{Binding Results}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="File" Binding="{Binding OriginalFileName}" Width="*"/>
                        <DataGridTextColumn Header="Channel" Binding="{Binding SourceChannelTitle}" Width="150"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding SizeBytes, Converter={StaticResource Bytes}}" Width="90"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Kind}" Width="80"/>
                        <DataGridTemplateColumn Header="Status" Width="110" SortMemberPath="Status">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Medium"
                                               Foreground="{Binding Status, Converter={StaticResource StatusBrush}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Txt" Binding="{Binding ExtractedTextFiles.Count}" Width="50"/>
                        <DataGridTextColumn Header="Processed (UTC)" Binding="{Binding ProcessedUtc, StringFormat=yyyy-MM-dd HH:mm}" Width="140"/>
                        <DataGridTextColumn Header="Error" Binding="{Binding Error}" Width="220"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
