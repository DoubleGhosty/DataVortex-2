<UserControl x:Class="DataVortex.App.Views.ChannelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI"
             TextElement.Foreground="{DynamicResource TextPrimary}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Channels" Style="{StaticResource Title}"/>
            <TextBlock Style="{StaticResource Caption}" Margin="0,4,0,0"
                       Text="Tick the channels/groups you want to archive, then Save. Only ticked channels are watched."/>
        </StackPanel>

        <DockPanel Grid.Row="1" Margin="0,16,0,14" LastChildFill="False">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Refresh dialogs" Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
                <Button Content="Select all" Style="{StaticResource Ghost}" Command="{Binding SelectAllCommand}" Margin="0,0,8,0"/>
                <Button Content="Deselect all" Style="{StaticResource Ghost}" Command="{Binding DeselectAllCommand}" Margin="0,0,8,0"/>
                <Button Content="Save selection" Command="{Binding SaveCommand}"/>
            </StackPanel>
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource Caption}" DockPanel.Dock="Right"
                       VerticalAlignment="Center"/>
        </DockPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="8">
            <DataGrid ItemsSource="{Binding Channels}" IsReadOnly="False">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Watch" Width="68">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsWatched, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                    <DataGridTemplateColumn Header="Type" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource StatusBadge}">
                                    <TextBlock Text="{Binding Kind}" FontSize="11" FontWeight="Medium"
                                               Foreground="{DynamicResource Accent}" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Members" Binding="{Binding ParticipantsCount}" Width="110"/>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
